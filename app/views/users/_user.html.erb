 <div class="col-6">
    <div class="card mb-3">
        <div class="card-body">
            <div class="col-2">
                <%= profile_image(user, "profile-medium")%>
            </div>
            <div class="col-10">
                <h6><%= link_to user.name, user_path(user)%></h6>
                <span><%= user.email%></span>
            </div>
        </div>
    </div>
</div>